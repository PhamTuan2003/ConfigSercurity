server:
  port: 8080

spring:
  datasource:
    jdbc-url: jdbc:mysql://localhost:3306/ezpark?useSSL=false&serverTimezone=UTC
    username: root
    password: mixi2004
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  jwt:
    secret_key: WoOXNLcTzDlQqjp2JzMNJ7+fi7UQ3O8FAselbtKQSgM=

  # ACL Configuration
  security:
    acl:
      # Cache configuration
      cache:
        enabled: true
        cache-type: map
      # Database configuration
      jdbc:
        initialize-schema: always
        data-source-ref: dataSource

logging:
  level:
    org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer: ERROR
